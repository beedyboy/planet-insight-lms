import { type ComponentStyleConfig } from '@chakra-ui/react';

export const AlertStyles: ComponentStyleConfig = {
  baseStyle: {
    container: {
      bgColor: 'info.bg',
      color: 'info.main',
      borderRadius: 'base',
      fontSize: 'sm'
    }
  }
};

export default AlertStyles;
